export const metadata = {
  title: "Create Your Linkify",
  description:
    "Build and customize your link-in-bio page with a visual editor. Choose from 5 premium templates, add unlimited links, and preview changes in real-time.",
  alternates: { canonical: "/create" },
  robots: { index: false, follow: true },
};

export default function CreateLayout({ children }) {
  return children;
}
