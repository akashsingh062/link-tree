export const metadata = {
  title: "Log In",
  description:
    "Log in to your Linkify account to manage your bio link page, update links, and customize your profile template.",
  alternates: { canonical: "/login" },
  robots: { index: false, follow: true },
};

export default function LoginLayout({ children }) {
  return children;
}
